name: Sample
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    outputs:
       matrix: ${{ steps.setmatrix.outputs.matrix }}        
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v3
        with: 
          node-version: "18.x"
      - name: Set Dynamic Matrix
        id: setmatrix
        run: |
           # matrixStringifiedObject="{\"include\":[{\"source\":\"alpine\",\"target\":\"oc.template.alpine.minimal\",\"dockerfile\":\"oc.template.alpine.minimal\"},{\"source\":\"ubuntu:18.04\",\"target\":\"oc.template.ubuntu.minimal.18.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"},{\"source\":\"ubuntu:20.04\",\"target\":\"oc.template.ubuntu.minimal.20.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"},{\"source\":\"ubuntu:22.04\",\"target\":\"oc.template.ubuntu.minimal.22.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"}]}"
           # matrixStringifiedObjectRules=$(node rules/json2nop.js rules/minimal.json)
           # echo "::set-output name=matrix::$matrixStringifiedObject"
           mininaljson=$(node rules/json2nop.js rules/minimal.json)
           echo "::set-output name=matrix::${mininaljson}"
  job2:
    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.job1.outputs.matrix) }}
    steps:
      - run: echo Run ${{ matrix.source }}
      - run: echo Run ${{ matrix.target }}
      - run: echo Run ${{ matrix.dockerfile }}
      
      - name: Checkout repository and submodules
        uses: actions/checkout@v2
        with:
          submodules: recursive
          
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
 
      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
         username: ${{ secrets.DOCKER_USERNAME }}
         password: ${{ secrets.DOCKER_TOKEN }}
      
      - name: Build and push ${{ matrix.target }}
        uses: docker/build-push-action@v3
        with:
          file: ${{ matrix.dockerfile }}
          context: .
          platforms: linux/amd64, linux/arm64
          build-args: |
            BASE_IMAGE=${{ matrix.source }}
          push: true
          tags: |
            abcdesktopio/${{ matrix.target }}:dev
  job3:
    needs: job2
    runs-on: ubuntu-latest
    outputs:
       matrix: ${{ steps.setmatrix.outputs.matrix }}        
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v3
        with: 
          node-version: "18.x"
      - name: Set Dynamic Matrix
        id: setmatrix
        run: |
           # matrixStringifiedObject="{\"include\":[{\"source\":\"alpine\",\"target\":\"oc.template.alpine.minimal\",\"dockerfile\":\"oc.template.alpine.minimal\"},{\"source\":\"ubuntu:18.04\",\"target\":\"oc.template.ubuntu.minimal.18.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"},{\"source\":\"ubuntu:20.04\",\"target\":\"oc.template.ubuntu.minimal.20.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"},{\"source\":\"ubuntu:22.04\",\"target\":\"oc.template.ubuntu.minimal.22.04\",\"dockerfile\":\"oc.template.ubuntu.minimal\"}]}"
           # matrixStringifiedObjectRules=$(node rules/json2nop.js rules/minimal.json)
           # echo "::set-output name=matrix::$matrixStringifiedObject"
           mininaljson=$(node rules/json2nop.js rules/level0.json)
           echo "::set-output name=matrix::${mininaljson}"
  job4:
    needs: job3
    runs-on: ubuntu-latest
    strategy:
     matrix: ${{ fromJson(needs.job3.outputs.matrix) }}
    steps:
      - run: echo Run ${{ matrix.source }}
      - run: echo Run ${{ matrix.target }}
      - run: echo Run ${{ matrix.dockerfile }}
      
      - name: Checkout repository and submodules
        uses: actions/checkout@v2
        with:
          submodules: recursive
          
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
 
      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
         username: ${{ secrets.DOCKER_USERNAME }}
         password: ${{ secrets.DOCKER_TOKEN }}
      
      - name: Build and push ${{ matrix.target }}
        uses: docker/build-push-action@v3
        with:
          file: ${{ matrix.dockerfile }}
          context: .
          platforms: linux/amd64, linux/arm64
          build-args: |
            BASE_IMAGE=${{ matrix.source }}
            TAG=dev
          push: true
          tags: |
            abcdesktopio/${{ matrix.target }}:dev
 

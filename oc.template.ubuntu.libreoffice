# default TAG is dev
ARG TAG=dev
ARG BASE_IMAGE
FROM ${BASE_IMAGE}:${TAG}

# add libreoffice
RUN apt-get update && apt-get install -y --no-install-recommends \
	  dbus-x11 \
	  libreoffice \
	  libreoffice-gnome \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
